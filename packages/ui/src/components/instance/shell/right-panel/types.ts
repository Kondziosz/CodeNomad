export type RightPanelTab = "changes" | "git-changes" | "git-graph" | "files" | "status"

export type DiffViewMode = "split" | "unified"

export type DiffContextMode = "expanded" | "collapsed"

export type DiffWordWrapMode = "on" | "off"
